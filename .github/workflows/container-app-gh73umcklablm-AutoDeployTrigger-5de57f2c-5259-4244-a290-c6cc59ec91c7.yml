name: Trigger auto deployment for container-app-gh73umcklablm

# When this action will be executed
on:
  # Automatically trigger it when detected changes in repo
  push:
    branches: 
      [ main ]
    paths:
    - '//**'
    - '.github/workflows/container-app-gh73umcklablm-AutoDeployTrigger-5de57f2c-5259-4244-a290-c6cc59ec91c7.yml'

  # Allow manual trigger 
  workflow_dispatch:      

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions: 
      id-token: write #This is required for requesting the OIDC JWT Token
      contents: read #Required when GH token is used to authenticate with private repo

    steps:
      - name: Checkout to the branch
        uses: actions/checkout@v2

      - name: Azure Login
        uses: azure/login@v1
        with:
          client-id: ${{ secrets.CONTAINERAPPGH73UMCKLABLM_AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.CONTAINERAPPGH73UMCKLABLM_AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.CONTAINERAPPGH73UMCKLABLM_AZURE_SUBSCRIPTION_ID }}

      - name: Build and push container image to registry
        uses: azure/container-apps-deploy-action@v2
        with:
          appSourcePath: ${{ github.workspace }} 
          registryUrl: 
          registryUsername: ${{ secrets.CONTAINERAPPGH73UMCKLABLM_REGISTRY_USERNAME }}
          registryPassword: ${{ secrets.CONTAINERAPPGH73UMCKLABLM_REGISTRY_PASSWORD }}
          containerAppName: container-app-gh73umcklablm
          resourceGroup: todonodejspgsqlgoogleautha6008334
          imageToBuild: default/container-app-gh73umcklablm:${{ github.sha }}
          _buildArgumentsKey_: |
            _buildArgumentsValues_


